class NodeWorker:
    # 每个 node 上运行的 client
    def __init__(self):
        # TODO 根据 node_profiler.py 中 go_through_every_shards方法 来构造
        #  然后重构 go_through_every_shards，反过来调用这里，实例化若干个 NodeWorker
        #  然后 NodeProfiler 再建新方法来做真正的 profile
        pass
